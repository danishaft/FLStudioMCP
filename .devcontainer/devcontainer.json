{
  "name": "flmcp dev",
  "image": "mcr.microsoft.com/devcontainers/python:3.13",
  "postCreateCommand": "pip install -e '.[dev]' && pip install 'mcp[cli]>=1.2.0,<2.0' && pip install PyFLP",
  "postStartCommand": "echo START >> /workspaces/FLStudioMCP/.sync-status.txt; { git clone -q https://danishaft:${OPENCODE_SYNC_TOKEN}@github.com/danishaft/opencode-sync.git /tmp/opencode-sync 2>&1 || git clone -q https://github.com/danishaft/opencode-sync.git /tmp/opencode-sync 2>&1; } > /tmp/clone.log; ls /tmp/opencode-sync/.git >/dev/null 2>&1 && echo CLONE-OK >> /workspaces/FLStudioMCP/.sync-status.txt || cp /tmp/clone.log /workspaces/FLStudioMCP/.clone-fail.txt; curl -fsSL https://opencode.ai/install | bash > /tmp/oc-install.log 2>&1; ls ~/.opencode/bin/opencode >/dev/null 2>&1 && echo INSTALL-OK >> /workspaces/FLStudioMCP/.sync-status.txt || cp /tmp/oc-install.log /workspaces/FLStudioMCP/.install-fail.txt; mkdir -p ~/.local/share/opencode && cp /tmp/opencode-sync/auth.json /tmp/opencode-sync/mcp-auth.json ~/.local/share/opencode/ 2>/dev/null; ~/.opencode/bin/opencode import /tmp/opencode-sync/session/ses_0082024d3ffe4VpdP51QewWLPM.json >> /workspaces/FLStudioMCP/.sync-status.txt 2>&1; echo IMPORT-DONE >> /workspaces/FLStudioMCP/.sync-status.txt",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "ms-python.black-formatter"]
    }
  }
}