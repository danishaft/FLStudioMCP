# Merlin — Patterns

_Reusable procedures. Cap: 20, newest on top, ≤8 lines each: trigger / steps / when NOT to use. Merge duplicates — a pattern that recurs three times is either a rule (promote to merlin.md) or noise (drop). When over cap, propose the prune — user approves._

- [2026-08-14] Song sprint — trigger: new track started. Steps: hook → sketch arrangement → draft vocals → production pass → A/B mix. When not: don't use for arrangement fixes on a finished song.
- [2026-08-14] Signature-move chain — trigger: production pass on any track. Steps: drums-first dial-in → drum-fx mangling → tape-print commit → vocal 8 kHz abuse → AM-radio ear-break → minimal bus. When not: don't stack all moves on one track — each move should earn its place.